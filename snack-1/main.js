console.log('hello');

const num_one = Number(prompt('digita un numero'));
const num_two = Number(prompt('digita un numero'));

if (num_one >= num_two) {
    console.log(num_one);
} else {
    console.log(num_two);
}